<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
        package="com.DO.Prediction.client">
    <!-- 这个权限用于进行网络定位 -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" /> <!-- 这个权限用于访问GPS定位 -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" /> <!-- 用于访问wifi网络信息，wifi信息会用于进行网络定位 -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" /> <!-- 获取运营商信息，用于支持提供运营商信息相关的接口 -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" /> <!-- 这个权限用于获取wifi的获取权限，wifi信息会用来进行网络定位 -->
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" /> <!-- 写入扩展存储，向扩展卡写入数据，用于写入离线定位数据 -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" /> <!-- 访问网络，网络定位需要上网 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="com.android.launcher.permission.READ_SETTINGS" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <application
            android:name="org.litepal.LitePalApplication"
            android:allowBackup="true"
            android:icon="@drawable/doapp"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme"
            android:usesCleartextTraffic="true">
        <activity android:name=".ChangePasswordActivity" />
        <activity android:name=".FeedBackActivity" />
        <activity android:name=".NewsMainActivity" />
        <activity android:name=".NewsDisplayActivity" />
        <activity android:name=".YuyeInfoActivity" />
        <activity android:name=".OutlierActivity" />
        <activity android:name=".PredictActivity" />
        <activity android:name=".ChartActivity" />
        <activity android:name=".HistoryActivity" />
        <activity android:name=".ShuiChanZhiDaoActivity" />
        <activity android:name=".ZaiHaiActivity" />

        <meta-data
                android:name="com.baidu.lbsapi.API_KEY"
                android:value="GbazwHIV2Y8n0E9rrqZqdiij6wBB0dIG" />

        <service
                android:name="com.baidu.location.f"
                android:enabled="true"
                android:process=":remote" />

        <activity android:name=".EightActivity" />
        <activity android:name=".SevenActivity" />
        <activity android:name=".SixActivity" />
        <activity android:name=".FiveActivity" />
        <activity android:name=".FourActivity" />
        <activity android:name=".ThreeActivity" />
        <activity android:name=".TwoActivity" />
        <activity android:name=".OneActivity" />
        <activity android:name=".LeftMenuActivity" />
        <activity
                android:name=".MenuActivity"></activity>
        <activity android:name=".RegisterActivity" />
        <activity android:name=".MainActivity"
                android:launchMode="singleTask"
                >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".coolweather.WeatherActivity" />
        <activity android:name=".coolweather.WeaMainActivity" />

        <service
                android:name=".coolweather.service.AutoUpdateService"
                android:enabled="true"
                android:exported="true" />
    </application>

</manifest>